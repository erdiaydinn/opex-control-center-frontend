name: EAY Release Candidate Integration

on:
  push:
    branches:
      - release/eay-rc-composition-v0.1
  pull_request:
    branches:
      - feature/phase-1-security-hardening
    paths:
      - "config/eay_release_candidate_v0_1.json"
      - "scripts/validate_eay_release_candidate.py"
      - ".github/workflows/eay-release-candidate-integration.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: eay-rc-integration-${{ github.ref }}
  cancel-in-progress: true

jobs:
  composition:
    name: Locked deployables, ancestry and platform composition
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Validate locked release composition
        id: composition
        continue-on-error: true
        run: |
          python scripts/validate_eay_release_candidate.py \
            --manifest config/eay_release_candidate_v0_1.json \
            --result eay-rc-composition-result.json
      - name: Upload composition evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: eay-rc-composition-evidence
          path: eay-rc-composition-result.json
          if-no-files-found: error
          retention-days: 14
      - name: Enforce composition gate
        if: steps.composition.outcome == 'failure'
        run: |
          cat eay-rc-composition-result.json
          exit 1
      - name: Materialize composed platform tree
        if: steps.composition.outcome == 'success'
        run: |
          SYNTHETIC_HEAD="$(python - <<'PY'
          import json
          result = json.load(open('eay-rc-composition-result.json'))
          print(result['deployables']['platform_release']['synthetic_composition_head'])
          PY
          )"
          git worktree add --detach /tmp/eay-rc-platform "$SYNTHETIC_HEAD"
      - name: Compile composed platform Python surfaces
        if: steps.composition.outcome == 'success'
        run: |
          python -m compileall -q /tmp/eay-rc-platform/backend/app /tmp/eay-rc-platform/services/core-api/app
      - name: Verify catalog coverage in composed platform tree
        if: steps.composition.outcome == 'success'
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          catalog = json.loads(Path('/tmp/eay-rc-platform/config/module_catalog.json').read_text())
          keys = {item['key'] for item in catalog['commercial_modules']}
          required = {'workforce', 'inventory', 'planogram', 'dockos', 'budget', 'academy', 'jarvis'}
          missing = required - keys
          assert not missing, f'module catalog missing canonical modules: {sorted(missing)}'
          PY
      - name: Assert AI deployable remains ancestry-bound
        if: steps.composition.outcome == 'success'
        run: |
          python - <<'PY'
          import json
          result = json.load(open('eay-rc-composition-result.json'))
          ai = result['deployables']['ai_service']
          assert ai['repository_integration_ready'] is True
          assert ai['artifact_head'] == result['locked_heads']['repository_intelligence']
          PY
