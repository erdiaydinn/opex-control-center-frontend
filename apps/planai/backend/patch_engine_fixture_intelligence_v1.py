from pathlib import Path

ENGINE = Path("engine.py")
BACKUP = Path("engine_before_fixture_intelligence_v1.py")
MARKER = "PLONAGRAM FIXTURE INTELLIGENCE v1 - APPENDED PATCH"

if not ENGINE.exists():
    raise FileNotFoundError("engine.py bulunamadı. Bu script backend klasöründe çalışmalı.")

text = ENGINE.read_text(encoding="utf-8")
if MARKER in text:
    print("Fixture Intelligence v1 zaten engine.py içinde var. Tekrar eklemedim.")
else:
    if not BACKUP.exists():
        BACKUP.write_text(text, encoding="utf-8")
    ENGINE.write_text(text.rstrip() + "
" + '\n\n# =====================================================\n# PLONAGRAM FIXTURE INTELLIGENCE v1 - APPENDED PATCH\n# Ürün artık önce fiziksel gerçekliğe girer: freezer door, chilled door,\n# produce crate, bulk floor, bakery station, water rack, hanging display,\n# non-food zone, top stock.\n# =====================================================\n\nFIXTURE_STORAGE_MAP = {\n    "REGULAR_SHELF": "AMBIENT",\n    "FREEZER_DOOR": "FROZEN",\n    "CHILLED_DOOR": "CHILLED",\n    "MILAGRO_CHILLED_PRODUCE": "CHILLED",\n    "PRODUCE_CRATE": "AMBIENT",\n    "BULK_FLOOR": "AMBIENT",\n    "WATER_RACK": "AMBIENT",\n    "DEMIJOHN_RACK": "AMBIENT",\n    "BAKERY_STATION": "AMBIENT",\n    "TOP_STOCK": "AMBIENT",\n    "HANGING_DISPLAY": "AMBIENT",\n    "NON_FOOD_CHEMICAL_ZONE": "AMBIENT",\n}\n\n# Existing functions are kept as base where useful.\n_BASE_ENRICH_PRODUCT_FIXTURE_V1 = enrich_product\n_BASE_GENERATE_DEFAULT_LAYOUT_FIXTURE_V1 = generate_default_layout\n_BASE_VALIDATE_PLANOGRAM_FIXTURE_V1 = validate_planogram\n\n\ndef _fx_norm(v):\n    return norm(v)\n\n\ndef _fx_hay(p):\n    return _fx_norm(\n        f"{product_name(p)} {brand(p)} {category_l1(p)} {category_l2(p)} "\n        f"{get(p, [\'category_l3\',\'Category L3\',\'pim_cat_l3\',\'frontend_category_l3\'], \'\')} "\n        f"{get(p, [\'storage_type\',\'storage_type_clean\',\'storage_type_raw\'], \'\')} "\n        f"{get(p, [\'package_type\',\'package_type_external\'], \'\')}"\n    )\n\n\ndef category_l3(p):\n    return clean_text(get(p, ["category_l3", "Category L3", "pim_cat_l3", "frontend_category_l3"], ""))\n\n\ndef storage_type(p: Dict[str, Any]) -> str:\n    """Category-first storage. Marka tek başına yetmez; category L1/L2/L3 ana hakemdir."""\n    hay = _fx_hay(p)\n\n    # hard frozen from taxonomy\n    if any(x in hay for x in [\n        "dondurulmus", "dondurulmuş", "donuk", "frozen", "freezer", "-18",\n        "ice cream", "dondurma", "frozen meat", "frozen bakery", "frozen vegetables",\n        "frozen dessert", "frozen desserts", "dondurulmus gida", "dondurulmuş gida",\n    ]):\n        return "FROZEN"\n\n    # ambient false positives before chilled\n    if any(x in hay for x in [\n        "sutlu cikolata", "sütlü çikolata", "peynir aromali", "peynir aromalı",\n        "peynirli cips", "ice tea", "soguk cay", "soğuk çay", "konserve",\n        "ton baligi", "ton balığı", "salca", "salça", "uht", "uzun omurlu", "uzun ömürlü",\n    ]):\n        return "AMBIENT"\n\n    # hard chilled\n    if any(x in hay for x in [\n        "chilled", "+4", "fridge", "cooler", "soguk", "soğuk", "dairy",\n        "sut urunleri", "süt ürünleri", "yogurt", "yoğurt", "ayran", "kefir",\n        "peynir", "sarkuteri", "şarküteri", "taze et", "fresh meat",\n        "sushi", "somon", "salmon", "paket salata", "salata mix",\n    ]):\n        return "CHILLED"\n\n    # produce chilled exceptions\n    if produce_handling_mode(p) in {"CHILLED_HERB", "CHILLED_LEAFY", "CHILLED_SENSITIVE"}:\n        return "CHILLED"\n\n    raw = key(get(p, ["storage_type", "storage_type_clean", "storage_type_raw", "Storage"], ""))\n    if raw in {"FROZEN", "CHILLED", "AMBIENT"}:\n        return raw\n    return "AMBIENT"\n\n\ndef produce_handling_mode(p: Dict[str, Any]) -> Optional[str]:\n    hay = _fx_hay(p)\n    is_produce = any(x in hay for x in [\n        "produce", "meyve", "sebze", "fruit", "vegetable", "vegetables", "fruits"\n    ])\n    if not is_produce:\n        return None\n\n    chilled_herbs = [\n        "maydanoz", "dereotu", "roka", "nane", "feslegen", "fesleğen", "tere",\n        "reyhan", "kuzukulağı", "kuzukulagi", "kişniş", "kisnis"\n    ]\n    chilled_leafy = [\n        "marul", "kivircik", "kıvırcık", "gobek", "göbek", "iceberg", "paket salata",\n        "salata mix", "akdeniz yesilligi", "akdeniz yeşilliği", "baby spinach", "ispanak", "ıspanak"\n    ]\n    chilled_sensitive = [\n        "mantar", "kultur mantari", "kültür mantarı", "brokoli", "karnabahar", "brüksel", "bruksel"\n    ]\n    ambient_root = ["patates", "sogan", "soğan", "sarimsak", "sarımsak", "havuc", "havuç", "pancar", "turp"]\n    ambient_citrus = ["mandalina", "portakal", "limon", "greyfurt", "narenciye"]\n    ambient_bulk = ["karpuz", "kavun", "balkabagi", "balkabağı"]\n    ambient_soft = ["muz", "avokado", "domates", "salatalik", "salatalık", "biber", "patlican", "patlıcan"]\n\n    if any(x in hay for x in chilled_herbs):\n        return "CHILLED_HERB"\n    if any(x in hay for x in chilled_leafy):\n        return "CHILLED_LEAFY"\n    if any(x in hay for x in chilled_sensitive):\n        return "CHILLED_SENSITIVE"\n    if any(x in hay for x in ambient_bulk):\n        return "AMBIENT_BULK"\n    if any(x in hay for x in ambient_root):\n        return "AMBIENT_ROOT"\n    if any(x in hay for x in ambient_citrus):\n        return "AMBIENT_CITRUS"\n    if any(x in hay for x in ambient_soft):\n        return "AMBIENT_PRODUCE"\n    return "AMBIENT_PRODUCE"\n\n\ndef fixture_need(p: Dict[str, Any]) -> str:\n    hay = _fx_hay(p)\n    existing = key(get(p, ["fixture_need", "recommended_zone_type"], ""))\n    if existing in FIXTURE_STORAGE_MAP:\n        return existing\n\n    pmode = produce_handling_mode(p)\n    if pmode in {"CHILLED_HERB", "CHILLED_LEAFY", "CHILLED_SENSITIVE"}:\n        return "MILAGRO_CHILLED_PRODUCE"\n    if pmode in {"AMBIENT_ROOT", "AMBIENT_CITRUS", "AMBIENT_PRODUCE"}:\n        return "PRODUCE_CRATE"\n    if pmode == "AMBIENT_BULK":\n        return "BULK_FLOOR"\n\n    st = storage_type(p)\n    if st == "FROZEN":\n        return "FREEZER_DOOR"\n    if st == "CHILLED":\n        return "CHILLED_DOOR"\n\n    # bakery station is operational fixture, not regular shelf\n    if any(x in hay for x in ["la lorraine", "firin", "fırın", "bakery", "ekmek", "baget", "kruvasan", "croissant"]):\n        return "BAKERY_STATION"\n\n    # water / demijohn / bulky beverages\n    if any(x in hay for x in ["damacana", "demijohn"]):\n        return "DEMIJOHN_RACK"\n    if any(x in hay for x in ["5 l", "5l", "10 l", "10l", "6x", "12x", "24x", "koli", "shrink", "case pack"]):\n        if any(y in hay for y in ["su", "water", "cola", "kola", "fanta", "sprite", "icecek", "içecek", "gazoz"]):\n            return "BULK_FLOOR"\n\n    # paper bulky\n    if any(x in hay for x in ["tuvalet kagidi", "tuvalet kağıdı", "havlu", "kagit havlu", "kağıt havlu", "pecete", "peçete"]):\n        return "BULK_FLOOR"\n\n    # hanging\n    if any(x in hay for x in ["dis fircasi", "diş fırçası", "askili", "askılı"]):\n        return "HANGING_DISPLAY"\n\n    # chemical/non-food odor\n    if any(x in hay for x in [\n        "air wick", "oda kokusu", "freshmatic", "domestos", "deterjan", "camasir", "çamaşır",\n        "yumusatici", "yumuşatıcı", "bleach", "temizleyici", "yuzey temizleyici", "yüzey temizleyici"\n    ]):\n        return "NON_FOOD_CHEMICAL_ZONE"\n\n    return "REGULAR_SHELF"\n\n\ndef fixture_viewer_type(fixture: str) -> str:\n    return {\n        "FREEZER_DOOR": "open_freezer_door_planogram",\n        "CHILLED_DOOR": "open_chilled_door_planogram",\n        "MILAGRO_CHILLED_PRODUCE": "milagro_chilled_produce_view",\n        "PRODUCE_CRATE": "produce_crate_bin_view",\n        "BULK_FLOOR": "bulk_floor_pallet_view",\n        "WATER_RACK": "water_rack_cell_view",\n        "DEMIJOHN_RACK": "demijohn_cell_view",\n        "BAKERY_STATION": "bakery_station_view",\n        "TOP_STOCK": "top_stock_buffer_view",\n        "HANGING_DISPLAY": "hanging_display_view",\n        "NON_FOOD_CHEMICAL_ZONE": "non_food_chemical_shelf_view",\n        "REGULAR_SHELF": "regular_shelf_view",\n    }.get(str(fixture or "REGULAR_SHELF"), "regular_shelf_view")\n\n\ndef merch_group(p: Dict[str, Any]) -> str:\n    fx = fixture_need(p)\n    hay = _fx_hay(p)\n    st = storage_type(p)\n    pmode = produce_handling_mode(p)\n\n    if fx == "FREEZER_DOOR" or st == "FROZEN":\n        return "FROZEN_FOOD"\n    if fx in {"CHILLED_DOOR", "MILAGRO_CHILLED_PRODUCE"} or st == "CHILLED":\n        return "CHILLED_FOOD"\n    if pmode:\n        return "PRODUCE"\n    if fx in {"BULK_FLOOR", "WATER_RACK", "DEMIJOHN_RACK"}:\n        return "BULK_BEVERAGE" if any(x in hay for x in ["su", "water", "cola", "kola", "icecek", "içecek"]) else "BULK_AMBIENT"\n    if fx == "BAKERY_STATION":\n        return "BAKERY"\n    if fx == "HANGING_DISPLAY":\n        return "HANGING_NONFOOD"\n    if fx == "NON_FOOD_CHEMICAL_ZONE":\n        return "NON_FOOD_ODOR"\n    if any(x in hay for x in ["sampuan", "şampuan", "deodorant", "dis macunu", "diş macunu", "ped", "kotex", "orkid"]):\n        return "PERSONAL_CARE"\n    if any(x in hay for x in ["cips", "chips", "lays", "ruffles", "doritos", "kraker"]):\n        return "SNACK_BAG"\n    if any(x in hay for x in ["konserve", "ton baligi", "ton balığı"]):\n        return "CANNED_FOOD"\n    return "FOOD_AMBIENT"\n\n\ndef _fixture_from_module_shelf(module: Dict[str, Any], shelf: Dict[str, Any]) -> str:\n    raw = key(\n        f"{module.get(\'fixture_type\',\'\')} {module.get(\'module_type\',\'\')} {module.get(\'recommended_zone_type\',\'\')} "\n        f"{shelf.get(\'fixture_type\',\'\')} {shelf.get(\'recommended_zone_type\',\'\')} {shelf.get(\'allowed_fixture_type\',\'\')} "\n        f"{shelf.get(\'allowed_storage_type\',\'\')}"\n    )\n    if "FREEZER" in raw or "-18" in raw or "FROZEN" in raw:\n        return "FREEZER_DOOR"\n    if "MILAGRO" in raw:\n        return "MILAGRO_CHILLED_PRODUCE"\n    if "FRIDGE" in raw or "CHILLED" in raw or "+4" in raw or "COOL" in raw:\n        return "CHILLED_DOOR"\n    if "PRODUCE" in raw or "CRATE" in raw:\n        return "PRODUCE_CRATE"\n    if "BULK" in raw or "PALLET" in raw:\n        return "BULK_FLOOR"\n    if "WATER" in raw or "DEMIJOHN" in raw:\n        return "DEMIJOHN_RACK"\n    if "BAKERY" in raw or "FIRIN" in raw or "FIRIN" in raw:\n        return "BAKERY_STATION"\n    if "HANGING" in raw:\n        return "HANGING_DISPLAY"\n    if "NON_FOOD" in raw or "CHEMICAL" in raw:\n        return "NON_FOOD_CHEMICAL_ZONE"\n    return "REGULAR_SHELF"\n\n\ndef fixture_compatible(product_fixture: str, shelf_fixture: str) -> bool:\n    pf = key(product_fixture)\n    sf = key(shelf_fixture)\n    if pf == sf:\n        return True\n    # Milagro chilled produce can live in a chilled door if there is no dedicated Milagro.\n    if pf == "MILAGRO_CHILLED_PRODUCE" and sf == "CHILLED_DOOR":\n        return True\n    # Water rack and bulk floor are operationally close, but never regular shelf for large packs.\n    if pf in {"WATER_RACK", "DEMIJOHN_RACK"} and sf == "BULK_FLOOR":\n        return True\n    return False\n\n\ndef enrich_product(raw: Dict[str, Any], allow_ai_dimensions: bool = True) -> Dict[str, Any]:\n    p = _BASE_ENRICH_PRODUCT_FIXTURE_V1(raw, allow_ai_dimensions=allow_ai_dimensions)\n    fx = fixture_need(p)\n    pmode = produce_handling_mode(p)\n    p["category_l3"] = category_l3(p)\n    p["storage_type"] = storage_type(p)\n    p["storage_type_final"] = p["storage_type"]\n    p["fixture_need"] = fx\n    p["recommended_zone_type"] = fx\n    p["fixture_viewer_type"] = fixture_viewer_type(fx)\n    p["produce_handling_mode"] = pmode\n    p["merch_group"] = merch_group(p)\n    p["_storage"] = p["storage_type"]\n    p["_fixture_need"] = fx\n    p["_merch_group"] = p["merch_group"]\n\n    # Product behavior flags used by viewer and replenishment logic\n    p["same_sku_backstock_allowed"] = fx not in {"HANGING_DISPLAY"}\n    p["top_stock_allowed"] = fx in {"REGULAR_SHELF", "NON_FOOD_CHEMICAL_ZONE", "BULK_FLOOR"}\n    p["food_safety_group"] = "NON_FOOD" if p["merch_group"].startswith("NON_FOOD") or p["merch_group"] in {"PERSONAL_CARE", "HANGING_NONFOOD"} else "FOOD"\n    p["odor_risk"] = p["merch_group"] == "NON_FOOD_ODOR"\n    return p\n\n\ndef is_food(p: Dict[str, Any]) -> bool:\n    return not (merch_group(p).startswith("NON_FOOD") or merch_group(p) in {"PERSONAL_CARE", "HANGING_NONFOOD"})\n\n\ndef is_odor(p: Dict[str, Any]) -> bool:\n    return merch_group(p) == "NON_FOOD_ODOR"\n\n\ndef generate_default_layout() -> Dict[str, Any]:\n    plan = _BASE_GENERATE_DEFAULT_LAYOUT_FIXTURE_V1()\n\n    for aisle in plan.get("aisles", []):\n        for module in aisle.get("modules", []):\n            mtype = key(module.get("module_type"))\n            if "FREEZER" in mtype:\n                module["fixture_type"] = "FREEZER_DOOR"\n                module["recommended_zone_type"] = "FREEZER_DOOR"\n                module["viewer_type"] = fixture_viewer_type("FREEZER_DOOR")\n            elif "FRIDGE" in mtype:\n                module["fixture_type"] = "CHILLED_DOOR"\n                module["recommended_zone_type"] = "CHILLED_DOOR"\n                module["viewer_type"] = fixture_viewer_type("CHILLED_DOOR")\n            else:\n                module.setdefault("fixture_type", "REGULAR_SHELF")\n                module.setdefault("recommended_zone_type", "REGULAR_SHELF")\n                module.setdefault("viewer_type", fixture_viewer_type("REGULAR_SHELF"))\n            for shelf in module.get("shelves", []):\n                shelf.setdefault("fixture_type", module.get("fixture_type"))\n                shelf.setdefault("recommended_zone_type", module.get("recommended_zone_type"))\n                shelf.setdefault("allowed_fixture_type", module.get("fixture_type"))\n\n    # Add operational fixture aisles if the layout does not have them.\n    existing = {key(a.get("aisle_id")) for a in plan.get("aisles", [])}\n\n    def _add_fixture_aisle(aid, fixture, storage, modules=2, shelves=4, width=120, depth=60, row=4, pos=1):\n        if key(aid) in existing:\n            return\n        plan.setdefault("aisles", []).append({\n            "aisle_id": aid,\n            "row": row,\n            "position": pos,\n            "direction": "SPECIAL",\n            "distance_to_dispatch": 99,\n            "aisle_type": "fixture_zone",\n            "zone_type": fixture,\n            "fixture_type": fixture,\n            "modules": [\n                {\n                    "module_id": i + 1,\n                    "side": "L",\n                    "module_type": fixture.lower(),\n                    "fixture_type": fixture,\n                    "recommended_zone_type": fixture,\n                    "viewer_type": fixture_viewer_type(fixture),\n                    "module_width_cm": width,\n                    "module_depth_cm": depth,\n                    "module_height_cm": 200,\n                    "assignment_rule": None,\n                    "shelves": [\n                        {\n                            "shelf_no": s + 1,\n                            "shelf_width_cm": width,\n                            "shelf_height_cm": 35,\n                            "shelf_depth_cm": depth,\n                            "max_weight_kg": 80 if fixture in {"BULK_FLOOR", "WATER_RACK", "DEMIJOHN_RACK"} else 45,\n                            "zone_type": "bottom" if s == 0 else "eye" if s in [1, 2] else "top",\n                            "allowed_storage_type": storage,\n                            "fixture_type": fixture,\n                            "recommended_zone_type": fixture,\n                            "allowed_fixture_type": fixture,\n                            "products": [],\n                            "used_width_cm": 0,\n                            "used_weight_kg": 0,\n                            "used": 0,\n                        } for s in range(shelves)\n                    ],\n                } for i in range(modules)\n            ],\n        })\n\n    _add_fixture_aisle("PRODUCE", "PRODUCE_CRATE", "AMBIENT", modules=3, shelves=3, width=120, depth=80, row=4, pos=1)\n    _add_fixture_aisle("MILAGRO", "MILAGRO_CHILLED_PRODUCE", "CHILLED", modules=2, shelves=4, width=120, depth=55, row=4, pos=2)\n    _add_fixture_aisle("BULK", "BULK_FLOOR", "AMBIENT", modules=3, shelves=2, width=160, depth=100, row=4, pos=3)\n    _add_fixture_aisle("WATER", "DEMIJOHN_RACK", "AMBIENT", modules=2, shelves=5, width=100, depth=70, row=4, pos=4)\n    _add_fixture_aisle("BAKERY", "BAKERY_STATION", "AMBIENT", modules=2, shelves=4, width=120, depth=60, row=4, pos=5)\n    _add_fixture_aisle("NONFOOD", "NON_FOOD_CHEMICAL_ZONE", "AMBIENT", modules=3, shelves=5, width=100, depth=50, row=5, pos=1)\n    _add_fixture_aisle("HANG", "HANGING_DISPLAY", "AMBIENT", modules=1, shelves=6, width=80, depth=20, row=5, pos=2)\n    return plan\n\n\ndef prepare_layout(layout: Optional[Dict[str, Any]]) -> Dict[str, Any]:\n    # If layout is provided, enrich fixture metadata instead of forcing default.\n    plan = deepcopy(layout or generate_default_layout())\n    for aisle in plan.get("aisles", []):\n        for module in aisle.get("modules", []):\n            if not module.get("fixture_type"):\n                module["fixture_type"] = _fixture_from_module_shelf(module, {"allowed_storage_type": module.get("storage_type", "AMBIENT")})\n            module.setdefault("recommended_zone_type", module.get("fixture_type", "REGULAR_SHELF"))\n            module.setdefault("viewer_type", fixture_viewer_type(module.get("fixture_type")))\n            for shelf in module.get("shelves", []):\n                shelf["products"] = []\n                shelf["used_width_cm"] = 0\n                shelf["used_weight_kg"] = 0\n                shelf["used"] = 0\n                shelf.setdefault("fixture_type", _fixture_from_module_shelf(module, shelf))\n                shelf.setdefault("recommended_zone_type", shelf.get("fixture_type"))\n                shelf.setdefault("allowed_fixture_type", shelf.get("fixture_type"))\n    return plan\n\n\ndef can_place(\n    p: Dict[str, Any],\n    aisle: Dict[str, Any],\n    module: Dict[str, Any],\n    shelf: Dict[str, Any],\n    aisle_groups: Optional[set] = None,\n) -> Tuple[bool, str]:\n    pf = p.get("_fixture_need") or fixture_need(p)\n    sf = _fixture_from_module_shelf(module, shelf)\n\n    if not fixture_compatible(pf, sf):\n        return False, f"fixture_not_fit:{pf}_to_{sf}"\n\n    if storage_score(p, aisle, module, shelf) < -1000:\n        return False, "storage_not_fit"\n\n    # Food safety wall. Non-food odor cannot share aisle with food, food cannot enter chemical zone.\n    mg = p.get("_merch_group") or merch_group(p)\n    if sf == "NON_FOOD_CHEMICAL_ZONE" and not (mg.startswith("NON_FOOD") or mg in {"PERSONAL_CARE", "HANGING_NONFOOD"}):\n        return False, "food_not_allowed_in_nonfood_chemical_zone"\n    if mg == "NON_FOOD_ODOR" and sf in {"REGULAR_SHELF", "PRODUCE_CRATE", "CHILLED_DOOR", "FREEZER_DOOR", "MILAGRO_CHILLED_PRODUCE", "BAKERY_STATION"}:\n        return False, "odor_not_allowed_in_food_zone"\n\n    if not module_rule_matches(p, module):\n        return False, "module_rule_not_match"\n\n    shelf_rule = shelf.get("assignment_rule")\n    if shelf_rule and not module_rule_matches(p, {"assignment_rule": shelf_rule}):\n        return False, "shelf_rule_not_match"\n\n    if not merch_compatible(p, aisle, aisle_groups):\n        return False, "merch_not_compatible"\n\n    if not dimension_fit(p, shelf):\n        return False, "dimension_not_fit"\n\n    if not capacity_fit(p, shelf):\n        return False, "capacity_not_fit"\n\n    if not weight_fit(p, shelf):\n        return False, "weight_not_fit"\n\n    return True, "ok"\n\n\ndef place_product_fast(\n    plan: Dict[str, Any],\n    p: Dict[str, Any],\n    shelf_pool: Dict[str, List[Dict[str, Any]]],\n    aisle_groups: Dict[str, set],\n    scoring_config: Optional[Dict[str, float]] = None,\n    brand_side_rules: Optional[Dict[str, str]] = None,\n) -> Tuple[bool, Optional[Dict[str, Any]], str]:\n    storage = p.get("_storage") or storage_type(p)\n    pf = p.get("_fixture_need") or fixture_need(p)\n    candidates = shelf_pool.get(storage, [])\n\n    # Hard fixture filter first. This is the main point of v1.\n    candidates = [x for x in candidates if fixture_compatible(pf, _fixture_from_module_shelf(x["module"], x["shelf"]))]\n    candidates = sorted(candidates, key=lambda x: (num(x["shelf"].get("used_width_cm"), 0), x.get("route", 9999)))[:80]\n\n    best = None\n    best_score = -10**18\n    last_reason = "no_fixture_candidate"\n\n    for item in candidates:\n        aisle, module, shelf = item["aisle"], item["module"], item["shelf"]\n        remaining_width = num(shelf.get("shelf_width_cm"), 100) - num(shelf.get("used_width_cm"), 0)\n        if remaining_width < width(p):\n            last_reason = "early_capacity_reject"\n            continue\n        aid = clean_text(aisle.get("aisle_id"))\n        groups = aisle_groups.get(aid, set())\n        ok, reason = can_place(p, aisle, module, shelf, groups)\n        if not ok:\n            last_reason = reason\n            continue\n        sc = placement_score(p, aisle, module, shelf, scoring_config, brand_side_rules)\n        # Prefer exact fixture match. Fallback compatible gets smaller bonus.\n        sc += 1000 if key(pf) == key(_fixture_from_module_shelf(module, shelf)) else 300\n        if sc > best_score:\n            best_score = sc\n            best = item\n\n    if not best:\n        return False, None, last_reason\n\n    aisle, module, shelf = best["aisle"], best["module"], best["shelf"]\n    f = preferred_facing(p, shelf)\n    u = used_width(p, shelf)\n    cap_units = total_capacity_units(p, shelf, f)\n    cov = coverage_days(p, shelf, f)\n    sf = _fixture_from_module_shelf(module, shelf)\n\n    placed = {\n        "sku": sku(p),\n        "barcode": clean_text(get(p, ["barcode", "product_barcodes"], "")),\n        "product_name": product_name(p),\n        "brand": brand(p),\n        "brand_name": brand(p),\n        "category_l1": category_l1(p),\n        "category_l2": category_l2(p),\n        "category_l3": category_l3(p),\n        "frontend_category_local": category_l1(p),\n        "frontend_subcategory_local": category_l2(p),\n        "image_url": image_url(p),\n        "storage_type": p.get("_storage"),\n        "storage_type_final": p.get("_storage"),\n        "merch_group": p.get("_merch_group"),\n        "fixture_need": pf,\n        "placed_fixture_type": sf,\n        "recommended_zone_type": pf,\n        "fixture_viewer_type": fixture_viewer_type(sf),\n        "produce_handling_mode": produce_handling_mode(p),\n        "abc_class": p.get("_abc"),\n        "tier": p.get("_tier"),\n        "front_tier": p.get("_tier"),\n        "sales_qty_7d": sales_7d(p),\n        "percent_stops": percent_stops(p),\n        "on_hand_qty": on_hand(p),\n        "width_cm": width(p),\n        "height_cm": height(p),\n        "depth_cm": depth(p),\n        "weight_kg": weight(p),\n        "case_pack_qty": case_pack(p),\n        "facing": f,\n        "facing_count": f,\n        "used_width_cm": round(u, 1),\n        "depth_units": depth_units(p, shelf),\n        "total_capacity_units": cap_units,\n        "coverage_days": cov,\n        "same_sku_backstock_allowed": p.get("same_sku_backstock_allowed", True),\n        "top_stock_allowed": p.get("top_stock_allowed", False),\n        "dimension_source": p.get("dimension_source"),\n        "dimension_confidence": p.get("dimension_confidence"),\n        "dimension_reason": p.get("dimension_reason"),\n        "aisle": aisle.get("aisle_id"),\n        "aisle_id": aisle.get("aisle_id"),\n        "module_id": module.get("module_id"),\n        "shelf_no": shelf.get("shelf_no"),\n        "position_order": len(shelf.get("products", [])) + 1,\n        "placement_score": round(best_score, 1),\n    }\n\n    shelf.setdefault("products", []).append(placed)\n    shelf["used_width_cm"] = round(num(shelf.get("used_width_cm"), 0) + u, 1)\n    shelf["used"] = shelf["used_width_cm"]\n    shelf["used_weight_kg"] = round(num(shelf.get("used_weight_kg"), 0) + weight(p) * f, 2)\n    aisle_groups.setdefault(clean_text(aisle.get("aisle_id")), set()).add(p.get("_merch_group"))\n    return True, placed, "ok"\n\n\ndef validate_planogram(plan: Dict[str, Any]) -> Dict[str, Any]:\n    base = _BASE_VALIDATE_PLANOGRAM_FIXTURE_V1(plan)\n    fixture_violations = []\n\n    for aisle in plan.get("aisles", []):\n        for module in aisle.get("modules", []):\n            for shelf in module.get("shelves", []):\n                sf = _fixture_from_module_shelf(module, shelf)\n                for p in shelf.get("products", []):\n                    pf = p.get("fixture_need") or fixture_need(p)\n                    if not fixture_compatible(pf, sf):\n                        fixture_violations.append({\n                            "type": "fixture_violation",\n                            "aisle_id": aisle.get("aisle_id"),\n                            "module_id": module.get("module_id"),\n                            "shelf_no": shelf.get("shelf_no"),\n                            "sku": p.get("sku"),\n                            "product_name": p.get("product_name"),\n                            "fixture_need": pf,\n                            "placed_fixture_type": sf,\n                            "message": f"{pf} ürünü {sf} fixture içine konamaz.",\n                        })\n\n    base["fixture_rule_violations"] = fixture_violations\n    base.setdefault("summary", {})["fixture_rule_violation_count"] = len(fixture_violations)\n    return base\n\n\n# Flag used by health/debugging\nFIXTURE_INTELLIGENCE_VERSION = "v1.0"\n' + "
", encoding="utf-8")
    print("Tamamlandı: engine.py içine Fixture Intelligence v1 eklendi.")
    print("Backup:", BACKUP)

print("Sonraki adım: python -m uvicorn main:app --host 127.0.0.1 --port 8001 --reload")
