"""EAY configurable workflow and policy engine.

The engine evaluates governed rules and emits action intents. It does not replace
module-owned lifecycle state machines or directly execute domain mutations.
"""
