# DockOS production gates

Production requires PostgreSQL persistence, corporate OIDC, signed gateway trust, BigQuery-only PO identity, durable notification retry/idempotency, backup/restore acceptance, and multi-worker concurrency tests. JSON mode is development-only.
