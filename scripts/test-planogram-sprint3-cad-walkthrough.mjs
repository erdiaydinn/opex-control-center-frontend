import fs from "node:fs";
import process from "node:process";

import { SUPPORTED_LOCALES } from "../src/platform/i18n/messages.js";
import { PLANOGRAM_AUTHORING_MESSAGES } from "../src/platform/i18n/planogramAuthoringMessages.js";
import { PLANOGRAM_WALKTHROUGH_MESSAGES } from "../src/platform/i18n/planogramWalkthroughMessages.js";
import { createPlanogramCadSession, executePlanogramCadSessionCommand, PLANOGRAM_CAD_SESSION_CONTRACT, redoPlanogramCadSession, undoPlanogramCadSession } from "../src/modules/planogram/planogramCadSession.js";
import { createStoreSceneNode, PLANOGRAM_STORE_SCENE_CONTRACT } from "../src/modules/planogram/planogramAuthoringModel.js";
import { buildPlanogramWalkthroughNavigation, isPlanogramWalkthroughPositionBlocked, PLANOGRAM_WALKTHROUGH_NAVIGATION_CONTRACT, resolvePlanogramWalkthroughStep } from "../src/modules/planogram/planogramWalkthroughNavigation.js";

function fail(message) { console.error(message); process.exit(1); }
const reviewedResult = { reviewed_draft_ready: true, reviewed_draft_fingerprint: "reviewed-sprint3-003", reviewed_store_dna_v2_preview: { review: { human_reviewed: true, scan_fingerprint: "scan-sprint3-003" }, architecture: { schema_version: 2, coordinate_system: "cartesian_m_centered_rect", source: "human_reviewed_store_scan", source_ref: "scan://SPRINT3/review-003", floor_width_m: 10, floor_depth_m: 8, elements: [{ element_id: "ENTRY", element_type: "picker_entry", center_x_m: 1, center_y_m: 1, width_m: 0.4, depth_m: 0.4, rotation_deg: 0, scan_source_element_id: "scan-entry-3" }, { element_id: "SCAN-WALL", element_type: "wall", center_x_m: 3, center_y_m: 3, width_m: 2, depth_m: 0.12, rotation_deg: 17, scan_source_element_id: "scan-wall-3", scan_confidence: 0.93 }] } } };
let session = createPlanogramCadSession({ reviewedResult, minimumAisleM: 1 });
if (!session || session.contract !== PLANOGRAM_CAD_SESSION_CONTRACT || session.scene.contract !== PLANOGRAM_STORE_SCENE_CONTRACT) fail("Sprint 3 canonical CAD session missing.");
const initialRevision = session.scene.revision; const initialDepth = session.historyDepth;
const noOp = executePlanogramCadSessionCommand(session, { commandId: "CAD-NOOP", type: "UPDATE_NODE", nodeId: "SCAN-WALL", expectedRevision: session.scene.revision, patch: { geometry: { centerXM: 3 } } });
if (noOp !== session || noOp.scene.revision !== initialRevision || noOp.historyDepth !== initialDepth) fail("No-op CAD edit polluted StoreScene revision history.");
session = executePlanogramCadSessionCommand(session, { commandId: "CAD-MOVE-SCAN-WALL", type: "UPDATE_NODE", nodeId: "SCAN-WALL", expectedRevision: session.scene.revision, patch: { geometry: { centerXM: 4.25, rotationDeg: 23 } } });
const projectedWall = session.candidate.store_dna.architecture.elements.find((row) => row.element_id === "SCAN-WALL");
if (!projectedWall || projectedWall.center_x_m !== 4.25 || projectedWall.rotation_deg !== 23 || projectedWall.scan_confidence !== 0.93 || projectedWall.scan_source_element_id !== "scan-wall-3") fail("StoreScene edit/provenance projection drifted.");
for (const [commandId, node] of [["CAD-FIX-A", createStoreSceneNode({ nodeId: "CAD-FIX-A", nodeType: "fixture", geometry: { centerXM: 5, centerYM: 5, widthM: 1, depthM: 0.6, heightM: 2, rotationDeg: 17 }, provenance: { source: "human", sourceRef: "cad-session" } })], ["CAD-FIX-B", createStoreSceneNode({ nodeId: "CAD-FIX-B", nodeType: "fixture", geometry: { centerXM: 6.4, centerYM: 5.3, widthM: 1, depthM: 0.6, heightM: 2, rotationDeg: -12 }, provenance: { source: "human", sourceRef: "cad-session" } })]]) session = executePlanogramCadSessionCommand(session, { commandId, type: "CREATE_NODE", node, expectedRevision: session.scene.revision });
const beforeBatchRevision = session.scene.revision; const beforeBatchA = session.scene.nodes.find((row) => row.nodeId === "CAD-FIX-A"); const beforeBatchB = session.scene.nodes.find((row) => row.nodeId === "CAD-FIX-B");
session = executePlanogramCadSessionCommand(session, { commandId: "CAD-BATCH-ALIGN-X", type: "UPDATE_NODES", expectedRevision: session.scene.revision, updates: [{ nodeId: "CAD-FIX-A", patch: { geometry: { centerXM: 5.7 } } }, { nodeId: "CAD-FIX-B", patch: { geometry: { centerXM: 5.7 } } }] });
if (session.scene.revision !== beforeBatchRevision + 1 || session.scene.nodes.find((row) => row.nodeId === "CAD-FIX-A").geometry.centerXM !== 5.7 || session.scene.nodes.find((row) => row.nodeId === "CAD-FIX-B").geometry.centerXM !== 5.7) fail("Atomic multi-node StoreScene update did not use one revision.");
session = undoPlanogramCadSession(session);
if (session.scene.nodes.find((row) => row.nodeId === "CAD-FIX-A").geometry.centerXM !== beforeBatchA.geometry.centerXM || session.scene.nodes.find((row) => row.nodeId === "CAD-FIX-B").geometry.centerXM !== beforeBatchB.geometry.centerXM) fail("Atomic multi-node undo did not restore the group.");
session = redoPlanogramCadSession(session);
if (session.scene.nodes.find((row) => row.nodeId === "CAD-FIX-A").geometry.centerXM !== 5.7 || session.scene.nodes.find((row) => row.nodeId === "CAD-FIX-B").geometry.centerXM !== 5.7) fail("Atomic multi-node redo drifted.");
session = executePlanogramCadSessionCommand(session, { commandId: "CAD-LOCK-A", type: "SET_LOCK", nodeId: "CAD-FIX-A", expectedRevision: session.scene.revision, locked: true });
const lockedRevision = session.scene.revision; const lockedB = session.scene.nodes.find((row) => row.nodeId === "CAD-FIX-B").geometry.centerYM; let batchRejected = false;
try { executePlanogramCadSessionCommand(session, { commandId: "CAD-BATCH-LOCKED", type: "UPDATE_NODES", expectedRevision: session.scene.revision, updates: [{ nodeId: "CAD-FIX-A", patch: { geometry: { centerYM: 6 } } }, { nodeId: "CAD-FIX-B", patch: { geometry: { centerYM: 6 } } }] }); } catch { batchRejected = true; }
if (!batchRejected || session.scene.revision !== lockedRevision || session.scene.nodes.find((row) => row.nodeId === "CAD-FIX-B").geometry.centerYM !== lockedB) fail("Locked batch transform was partially applied instead of atomically rejected.");
session = executePlanogramCadSessionCommand(session, { commandId: "CAD-UNLOCK-A", type: "SET_LOCK", nodeId: "CAD-FIX-A", expectedRevision: session.scene.revision, locked: false });
session = executePlanogramCadSessionCommand(session, { commandId: "CAD-EDGE", type: "CREATE_NODE", expectedRevision: session.scene.revision, node: createStoreSceneNode({ nodeId: "CAD-EDGE", nodeType: "fixture", geometry: { centerXM: 9.4, centerYM: 2, widthM: 1, depthM: 0.6, heightM: 2 }, provenance: { source: "human" } }) });
session = executePlanogramCadSessionCommand(session, { commandId: "CAD-RESIZE", type: "RESIZE_FLOOR", expectedRevision: session.scene.revision, widthM: 9, depthM: 8 }); if (session.diagnostics.boundaryViolationCount < 1) fail("Floor resize did not expose boundary diagnostics."); session = undoPlanogramCadSession(session); if (session.scene.floor.widthM !== 10) fail("Floor resize undo drifted.");

const sceneModel = { contract: "eay.planogram.unified-twin-scene.v1", sourceKind: "reviewed_store_scan_preview", geometryAuthority: "reviewed_scan_preview_not_store_dna_authority", productionReleaseAllowed: false, floor: { widthM: 10, depthM: 8 }, architecture: [{ id: "ENTRY", type: "picker_entry", centerXM: 1, centerYM: 1, widthM: 0.4, depthM: 0.4, rotationDeg: 0 }, { id: "WALL", type: "wall", centerXM: 3, centerYM: 1, widthM: 0.2, depthM: 2, rotationDeg: 0 }], fixtures: [{ id: "FIX-1", moduleKey: "A:1", fixtureType: "REGULAR_SHELF", centerXM: 5, centerYM: 4, widthM: 1, depthM: 0.6, heightM: 2, rotationDeg: 30, products: [] }] };
const navigation = buildPlanogramWalkthroughNavigation(sceneModel); if (!navigation || navigation.contract !== PLANOGRAM_WALKTHROUGH_NAVIGATION_CONTRACT || navigation.productionReleaseAllowed !== false || isPlanogramWalkthroughPositionBlocked(navigation, navigation.start)) fail("Walk-through navigation authority drifted.");
if (!resolvePlanogramWalkthroughStep(navigation, { x: 2.4, y: 1 }, { x: 0.7, y: 0 }).blocked) fail("Walk-through wall collision drifted.");
for (const table of [PLANOGRAM_AUTHORING_MESSAGES, PLANOGRAM_WALKTHROUGH_MESSAGES]) { const englishKeys = Object.keys(table.en).sort(); for (const { code } of SUPPORTED_LOCALES) if (JSON.stringify(Object.keys(table[code] || {}).sort()) !== JSON.stringify(englishKeys)) fail(`Sprint 3 localization drifted: ${code}`); }
const authoring = fs.readFileSync("src/modules/planogram/PlanogramArchitecturalAuthoring.jsx", "utf8"); const cad = fs.readFileSync("src/modules/planogram/planogramCadSession.js", "utf8"); const walk = fs.readFileSync("src/modules/planogram/PlanogramFirstPersonWalkthrough.jsx", "utf8");
for (const needle of ['type: "UPDATE_NODES"', "selectedIds", "alignSelection", "nudgeSelection", "shiftKey", "ctrlKey", "metaKey", "drag.deltaX", "drag.deltaY", "session.diagnostics"]) if (!authoring.includes(needle)) fail(`Sprint 3 multi-select CAD UI capability missing: ${needle}`);
for (const needle of ['command.type === "UPDATE_NODES"', "PLANOGRAM_CAD_BATCH_LIMIT", "changed: false", "updates.length > PLANOGRAM_CAD_BATCH_LIMIT"]) if (!cad.includes(needle)) fail(`Sprint 3 atomic CAD session capability missing: ${needle}`);
for (const forbidden of ["updatePlanogramAuthoringElement(", "removePlanogramAuthoringElement(", "resizePlanogramAuthoringFloor("]) if (authoring.includes(forbidden)) fail(`CAD editor regressed to legacy document mutation: ${forbidden}`);
for (const needle of ['import("three/examples/jsm/controls/PointerLockControls.js")', "resolvePlanogramWalkthroughStep", 'dataset.productionReleaseAllowed = "false"']) if (!walk.includes(needle)) fail(`First-person runtime capability missing: ${needle}`);
console.log("SPRINT3_CAD_NOOP_HISTORY_SUPPRESSION=PASS");
console.log("SPRINT3_CAD_ATOMIC_MULTI_NODE_TRANSFORM=PASS");
console.log("SPRINT3_CAD_LOCKED_BATCH_FAIL_CLOSED=PASS");
console.log("SPRINT3_CAD_MULTISELECT_ALIGN_NUDGE_UI=PASS");
console.log("SPRINT3_COLLISION_AWARE_FIRST_PERSON_WALKTHROUGH=PASS");
